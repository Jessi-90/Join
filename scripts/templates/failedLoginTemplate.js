/**
 * Returns the HTML template for a failed login message.
 * @returns {string} The HTML string for the failed login message.
 */
function failedLoginTemplate() {
    return `
    <br>
    <span class="failed-login">
    Check your email and password. Please try again.
    </span>
    `
}